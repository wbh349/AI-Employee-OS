# OpenAI API Key — required to run the agent.
# Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_api_key_here

# Optional: address the /analyze server binds to (used by the n8n workflow)
HR_AGENT_HOST=0.0.0.0
HR_AGENT_PORT=8000
